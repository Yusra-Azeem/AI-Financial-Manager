# Used at BUILD time (Vite inlines these into the bundle — don't put
# secrets here, this ships to the browser).
VITE_API_BASE_URL=https://api.yourdomain.com
VITE_USE_MOCK=false
VITE_DEMO_CUSTOMER_ID=1
